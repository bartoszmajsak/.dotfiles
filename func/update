update() {
  sudo dnf update -y
  asdf update
  asdf plugin-update --all
  zplug update 
  sudo wget -P /usr/bin https://raw.githubusercontent.com/so-fancy/diff-so-fancy/master/third_party/build_fatpack/diff-so-fancy && sudo chmod +x /usr/bin/diff-so-fancy
}
