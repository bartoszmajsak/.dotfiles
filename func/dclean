dclean() {
  docker rm -v $(docker ps -a -q -f status=exited)
  docker rm -v $(docker ps -a -q -f dangling=true) 
  docker rmi $(docker images -f "dangling=true" -q)  
  docker volume rm $(docker volume ls -q | grep -v m2_repo)  
}
