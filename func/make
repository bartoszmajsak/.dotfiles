make() {
  local project_name=${PWD##*/}
  local timer=${timer:-$SECONDS}
  local make_cmd="$*"
  command make "$@"; notify_build_status $? "$(($SECONDS - $timer))" "$project_name [make $make_cmd]"
}
